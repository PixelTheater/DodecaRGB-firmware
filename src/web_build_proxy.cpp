// This is a placeholder file for the web build.
// The actual build is performed by the Makefile in the web directory.
// This file exists only to satisfy PlatformIO's need for a source file.

#ifdef PLATFORM_WEB
int main() {
    // The real main function is in web_simulator.cpp
    // This is never actually compiled or used
    return 0;
}
#endif 