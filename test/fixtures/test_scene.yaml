# Test case for integration tests
name: Test Scene
description: "YAML EXAMPLE: Generated from test_scene.yaml"

parameters:
  speed:
    type: ratio
    default: 0.5
    flags: [clamp]
    description: "Animation speed"
  
  count:
    type: count
    range: [10, 1000]
    default: 50
    flags: [wrap]
    description: "Number of particles"
  
  enabled:
    type: switch
    default: true
    description: "Enable animation" 